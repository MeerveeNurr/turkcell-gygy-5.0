spring:
    application:
      name: librarysystem
    datasource:
      url: jdbc:postgresql://localhost:5432/librarysystem
      username: admin
      password: admin
      driver-class-name: org.postgresql.Driver
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
